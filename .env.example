# Webhost AI — environment variables (copy to .env, never commit real values)

# Cloudflare (production hosting — PRE-4). Obtain from CEO once account exists.
CLOUDFLARE_API_TOKEN=
CLOUDFLARE_ACCOUNT_ID=

# Outreach tooling (PRE-5) — LLM + lead sources. Fill when those tasks start.
ANTHROPIC_API_KEY=
